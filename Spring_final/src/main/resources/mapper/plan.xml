<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.spring.model.Activity.CartDAO">
  
  	<insert id="cartadd" parameterType="com.spring.model.Activity.CartDTO">
		<!-- 
			<selectKey resultType="String" keyProperty="prod_id" order="BEFORE">
			select max(board_no) from board
		</selectKey>
		 -->
		<![CDATA[
			insert into Trv_Cart values((select count(c_no) from Trv_Cart)+1, #{U_id}, '#'||#{Prod_no})
		]]>
	</insert>
  
  </mapper>